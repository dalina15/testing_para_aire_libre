<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testing_para_aire_libre">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/aire_libre_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/aire_libre_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testing_para_aire_libre">
        <activity android:name=".Registro"></activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>